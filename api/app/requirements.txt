# Core FastAPI and web framework
fastapi==0.115.11
uvicorn==0.34.0
starlette==0.46.2

# Pydantic for data validation
pydantic==2.11.5
pydantic_core==2.33.2
annotated-types==0.7.0
typing_extensions==4.13.2

# Database drivers
asyncpg==0.30.0
# Note: For macOS, if psycopg2-binary fails, install system dependencies first:
# brew install openssl libpq
# export LDFLAGS="-L$(brew --prefix openssl)/lib -L$(brew --prefix libpq)/lib"
# export CPPFLAGS="-I$(brew --prefix openssl)/include -I$(brew --prefix libpq)/include"
# Then: pip install psycopg2-binary
# Alternative: Use psycopg2 (non-binary) which requires: brew install postgresql
psycopg2-binary==2.9.10

# SQLAlchemy ORM
SQLAlchemy==2.0.39

# MongoDB drivers (if needed)
motor==3.7.0
pymongo==4.13.0

# MySQL drivers (if needed)
aiomysql==0.2.0
PyMySQL==1.1.1

# Authentication and security
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
pyotp==2.9.0
bcrypt==3.2.2
itsdangerous>=2.1.2
authlib>=1.2.1
cryptography>=43.0.0

# CLI and utilities
typer==0.12.5
click==8.1.8
python-dotenv==1.0.1

# HTTP and networking
anyio==4.9.0
h11==0.16.0
idna==3.10
dnspython==2.7.0
async-timeout==5.0.1
httpx>=0.27.0

# System utilities
sniffio==1.3.1
exceptiongroup==1.3.0
typing-inspection==0.4.1

# AWS SDK for S3 operations
boto3==1.35.0
botocore==1.35.0